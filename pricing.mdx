---
title: Pricing
icon: credit-card
iconType: light
description: Understand credit-based billing.
---

import { companyName } from '/snippets/whitelabel/config.mdx';

## Credit system

API access is based on prepaid credits. **{companyName}** decrements your balance only for
successful API calls. Most calls cost **1 credit** each or are **free**:

| Service   | Base credits |
| :-------- | :----------- |
| `search`  | **1**        |
| `browser` | **1**        |
| `usage`   | **0 (free)** |
| `users`   | **0 (free)** |

A few premium parameters multiply the base cost of calls that use them:

| Service   | Key          | Value        | Credit multiplier |
| :-------- | :----------- | :----------- | :---------------- |
| `browser` | `difficulty` | `medium`     | **2**             |
| `browser` | `speed`      | `ridiculous` | **1.5**           |

For example, successfully calling the `browser` service with both `difficulty=medium` and
`speed=ridiculous` params would cost **3 credits** in total:

<Latex>1 base credit x 2 x 1.5 = 3 total credits</Latex>
