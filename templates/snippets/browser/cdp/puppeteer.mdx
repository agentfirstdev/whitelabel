```js
#!/usr/bin/env node
import puppeteer from 'puppeteer';

const browser = await puppeteer.connect({
  browserWSEndpoint: 'wss://{{API_ENDPOINT}}/browser',
  headers: {
    Authorization: `Bearer ${process.env.{{COMPANY_SLUG}}_TOKEN}`
  }
});
```
