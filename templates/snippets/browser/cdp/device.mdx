```js
const browser = await puppeteer.connect({
  browserWSEndpoint:
    'wss://{{API_ENDPOINT}}/browser' +
    '?device=Blackberry+Playbook',
  headers: { Authorization: `Bearer ${process.env.{{COMPANY_SLUG}}_TOKEN}` }
});
```
