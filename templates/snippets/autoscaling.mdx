export const Autoscaling = () => {
  return (
    <Warning>
      This endpoint autoscales; if you receive a `503` response, maintain traffic and expect errors
      to resolve within a few minutes.
    </Warning>
  );
};
