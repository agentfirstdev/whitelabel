export const Throttling = () => {
  return (
    <Warning>
      This endpoint is rate limited; if you receive a `429` response, pause for the backoff time
      required by the `Retry-After` header before making another request.
    </Warning>
  );
};
