---
title: Reporting
icon: chart-line
iconType: light
description: Track API usage and performance.
---

import Dates from '/snippets/whitelabel/reporting/dates.mdx';
import Datetimes from '/snippets/whitelabel/reporting/datetimes.mdx';
import Service from '/snippets/whitelabel/reporting/service.mdx';
import Services from '/snippets/whitelabel/reporting/services.mdx';
import Emails from '/snippets/whitelabel/reporting/emails.mdx';

## Consumption data

You can request hourly metrics by specifying start and end dates or times:

<Dates />

<Accordion icon='terminal' title='View time-range example'>
  <Datetimes />
</Accordion>

The API will respond with detailed JSON:

```json
[{
  "email": "user@example.com",
  "from": "2025-12-24T00:00:00+00:00",
  "to": "2025-12-25T00:00:00+00:00",
  "service": "browser",
  "result": "success",
  "calls": 17208,
  "meanSecs": 6.78
}, {
  "email": "user@example.com",
  "from": "2025-12-24T07:00:00+00:00",
  "to": "2025-12-24T15:00:00+00:00",
  "service": "browser",
  "result": "failure",
  "calls": 18,
  "meanSecs": 60.22
}]
```

You can also filter the metrics by one or more services:

<Service />

<Accordion icon='terminal' title='View multiple-services example'>
  <Services />
</Accordion>

Here are the available reporting keys and values:

| Key       | Required | Value                                                                                                                                                                                                                |
| :-------- | :------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `from`    | ✅       | The [ISO datetime](https://www.w3.org/TR/NOTE-datetime) to query data starting from, inclusively                                                                                                                     |
| `to`      | ✅       | The [ISO datetime](https://www.w3.org/TR/NOTE-datetime) to query data up to, inclusively                                                                                                                             |
| `email`   | ⬜       | **[Whitelabelers only]** The (case-insensitive) email address of the user to query data for; multiple email addresses can be provided by repeating the key; data for all email addresses will be included by default |
| `service` | ⬜       | The service to query data for, `search` or `browser`; multiple services can be provided by repeating the key; data for all services will be included by default                                                      |

<Warning>
  This endpoint is rate limited; if you receive a `429` response, pause for the backoff time given
  by the `Retry-After` header before making another request.
</Warning>

## Additional examples

<Emails />
